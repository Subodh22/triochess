module.exports = {
  time            : 5, // time control (MINUTES)
  additional_time : 1,  // time advance per move (SECONDS)

  port : process.env.PORT || 2000          // port on which server runs
}
